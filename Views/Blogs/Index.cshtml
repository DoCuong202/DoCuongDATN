@model PagedList.PagedList<BanHangOnline.Models.EF.Posts>
@using PagedList.Mvc;
@using PagedList;
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<link href="~/Content/Assets/styles/main_styles.css" rel="stylesheet" />
<style>
    .blog_item_t {
        border-color: #d1d1d1;
        border-radius: 4px;
        border-style: solid;
        border-width: 1px;
        display: flex;
        flex-direction: column;
        cursor: pointer;
        grid-area: auto;
        line-height: 32px;
        padding: 8px;
        height: 350px;
        margin-bottom: 40px;
    }

        .blog_item_t:hover blog-img-src {
            width: 150%
        }

    .blog-img {
        width: 100%;
        height: 200px;
        overflow: hidden;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 5px;
    }

    .blog-img-src {
        transition: 0.5s;
        width: 100%;
        height: 100%;
        object-fit: cover; /* Chọn object-fit phù hợp (cover, contain, scale-down, etc.) */
    }

    .blog_title_t {
        display: -webkit-box;
        font-size: 15px;
        font-weight: 700;
        grid-area: auto;
        line-height: 18px;
        height: 50px;
        margin: 10px 0px 0px;
        text-align: left;
        overflow: hidden;
    }

    .blog_item_t a {
        color: #2e2e2e;
    }

    .blog_des {
        color: #353535;
        display: -webkit-box;
        font-weight: 500;
        grid-area: auto;
        line-height: 24px;
        margin: 16px 0px;
        height: 130px;
    }

    .blog_createday {
        color: #362926;
        display: inline;
        grid-area: auto;
        line-height: 24px;
        height: 30px;
    }

    .header-blog {
        text-align: center;
        width: 80%;
        margin-left: 10%;
        margin-bottom: 30px;
        color: #353535;
    }
</style>

<div class="main_slider" style="background-image: url(Content/Assets/images/bg_chung_toi.png)">
    <div class="container fill_height">
        <div class="row align-items-center fill_height">
            <div class="col">
                <div class="main_slider_content">
                    <h1>Get up to 30% Off New Arrivals</h1>
                    <h6>Spring / Summer Collection 2017</h6>
                    <div class="red_button shop_now_button"><a href="#">read now</a></div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="container" style="margin-top: 50px;">
    <div class="title_blog text-center">
        Kumihimo là nơi giao thoa của kiệt tác ẩm thực đương đại và những kết nối độc đáo, chúng tôi mong muốn <br />mang lại cho thực khách Hà Thành
        một hành trình ẩm thực khó quên.​ Hãy đón chờ những câu chuyện cảm <br />hứng dần được bật mí và trở thành một
        trong những vị khách đầu tiên khám phá sự chuyển mình này nhé!
    </div>
    <div class="row mt-5">
        <div class="col-md-8"></div>
        @*<div class="col-md-4 float_left">
                @using (Html.BeginForm("Index", "Blogs", FormMethod.Get, new { }))
                {
                    <div class="input-group mb-3">
                        @Html.TextBox("searchText", null, new { @class = "form-control", placeholder = "Nhập từ khóa tìm kiếm" })
                        <button type="submit" class="btn btn-sm btn-info mx-4">Tìm kiếm</button>
                    </div>
                }
            </div>*@
    </div>

    <div class="row">
        @if (Model != null && Model.Any())
        {
            var i = ((ViewBag.page - 1) * ViewBag.size) + 1;
            foreach (var item in Model)
            {
                <div class="col-md-3 blog_item_col_t">
                    <div class="blog_item_t">
                        <div class="blog-img">
                            <img class="blog-img-src" src="@item.Image" />
                        </div>
                        <h4 class="blog_title_t"> <a href="cauchuyenamthuc/@item.Alias-@item.Id"> @item.Title</a></h4>
                        <p class="blog_des"> @item.Description</p>
                        <div class="d-flex justify-content-between">
                            <span class="blog_createday">
                                <i class="fa fa-clock-o"></i>
                                @item.CreatedDate.Day/@item.CreatedDate.Month/@item.CreatedDate.Year
                            </span>
                            <span><a href="cauchuyenamthuc/@item.Alias-@item.Id" target="_blank">Xem chi tiet</a></span>
                        </div>
                    </div>
                </div>
            }
        }
        else
        {
            <h4>Không có bản ghi nào</h4>
        }


    </div>

    @Html.PagedListPager(Model, page => Url.Action("Index", new { page }))

</div>
